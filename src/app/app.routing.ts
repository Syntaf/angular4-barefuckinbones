import { RouterModule, Routes } from '@angular/router';

// TODO: implement these routes
const routes: Routes = [
  // { path: '/mypath', component: /* new component here */ },
];

export const routing = RouterModule.forRoot(routes);
